define.model('model.Authentication', function (model, ModelUtil, require) {

  var Account = require('model.entity.Account');
  var AccessToken = require('model.entity.AccessToken');

  model.login = function (username, role, password) {

  }

});
